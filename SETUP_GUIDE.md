# دليل إعداد نظام الحضور والغياب - خبراء العطور الدولية

## 🚀 خطوات التشغيل

### 1. إعداد قاعدة البيانات

قم بتشغيل السكريبتات بالترتيب التالي في Supabase SQL Editor:

\`\`\`sql
-- الخطوة 1: إنشاء الجداول
-- قم بتشغيل: scripts/001_create_database_schema.sql

-- الخطوة 2: إضافة سياسات الأمان
-- قم بتشغيل: scripts/002_create_rls_policies.sql

-- الخطوة 3: إضافة البيانات الأساسية
-- قم بتشغيل: scripts/003_seed_initial_data.sql

-- الخطوة 4: إضافة البيانات التجريبية
-- قم بتشغيل: scripts/005_simple_test_data.sql
\`\`\`

### 2. إنشاء حسابات المستخدمين

بعد تشغيل السكريبتات، قم بإنشاء الحسابات التالية في Supabase Auth:

1. اذهب إلى **Authentication** > **Users** في لوحة تحكم Supabase
2. اضغط على **Add user** > **Create new user**
3. أنشئ الحسابات التالية:

| البريد الإلكتروني | كلمة المرور | الدور |
|-------------------|-------------|-------|
| manager@ipe.com | Test@123456 | مدير عام |
| supervisor@ipe.com | Test@123456 | مشرف المبيعات |
| employee1@ipe.com | Test@123456 | موظف مبيعات |
| employee2@ipe.com | Test@123456 | موظف مبيعات |
| employee3@ipe.com | Test@123456 | أمين مخزن |
| accountant@ipe.com | Test@123456 | محاسب |

### 3. ربط الحسابات بالموظفين

بعد إنشاء الحسابات، قم بتشغيل هذا الاستعلام لربط حسابات Auth بسجلات الموظفين:

\`\`\`sql
-- ربط حساب المدير
update public.employees 
set user_id = (select id from auth.users where email = 'manager@ipe.com')
where email = 'manager@ipe.com';

-- ربط حساب المشرف
update public.employees 
set user_id = (select id from auth.users where email = 'supervisor@ipe.com')
where email = 'supervisor@ipe.com';

-- ربط حساب الموظف 1
update public.employees 
set user_id = (select id from auth.users where email = 'employee1@ipe.com')
where email = 'employee1@ipe.com';

-- ربط حساب الموظف 2
update public.employees 
set user_id = (select id from auth.users where email = 'employee2@ipe.com')
where email = 'employee2@ipe.com';

-- ربط حساب الموظف 3
update public.employees 
set user_id = (select id from auth.users where email = 'employee3@ipe.com')
where email = 'employee3@ipe.com';

-- ربط حساب المحاسب
update public.employees 
set user_id = (select id from auth.users where email = 'accountant@ipe.com')
where email = 'accountant@ipe.com';
\`\`\`

### 4. تشغيل التطبيق

الآن يمكنك تسجيل الدخول باستخدام أي من الحسابات أعلاه!

## 📊 البيانات التجريبية المتوفرة

- ✅ 4 أقسام (المبيعات، الإدارة، المخازن، المحاسبة)
- ✅ 6 موظفين بصلاحيات مختلفة
- ✅ فترتين عمل (صباحية ومسائية)
- ✅ 60 سجل حضور (20 يوم لكل من 3 موظفين)
- ✅ 9 معايير تقييم (5 عامة + 4 خاصة بالمبيعات)
- ✅ 2 تقييم كامل مع التفاصيل

## 🔧 استكشاف الأخطاء

### خطأ: "column position does not exist"
- تأكد من تشغيل `001_create_database_schema.sql` أولاً
- السكريبت يحتوي على عمود `position` في جدول الموظفين

### خطأ: "null value in column employee_number"
- تأكد من تشغيل `005_simple_test_data.sql` بدلاً من `004_seed_test_data.sql`
- السكريبت الجديد يحتوي على جميع الأعمدة المطلوبة

### لا يمكن تسجيل الدخول
- تأكد من إنشاء الحسابات في Supabase Auth
- تأكد من ربط الحسابات بسجلات الموظفين (الخطوة 3)
- تأكد من تشغيل `002_create_rls_policies.sql`

## 📞 الدعم الفني

**برمجة وتصميم:**
- الولي سوفت (Alwalisoft)
- البريد الإلكتروني: alwalisoftt@gmail.com
- الهاتف: +967777670507

---

**© 2025 خبراء العطور الدولية - International Perfume Experts**
